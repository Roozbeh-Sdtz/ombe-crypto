syntax = "proto3";

package iecrypto;

service Iecrypto {
    rpc identifierOfKey(publikkey) returns (identifier) {};
    rpc insert(insertModel) returns (insertResult) {};
}

message insertModel {
    string pubkey = 1;
    string identifier = 2;
}

message insertResult {
    string status = 1;
}

message publikkey {
    string pubkey = 1;
}

message identifier {
    int32 status = 1;
    string key = 2;
}